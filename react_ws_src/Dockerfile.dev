# Dockerfile for react_ws_src (development)
FROM node:14

WORKDIR /app

# Install dependencies (will be re-run if package.json changes)
COPY package.json ./

RUN npm install

# Mount source code for live editing
VOLUME ["/app"]

# Expose dev server port (adjust if needed)
EXPOSE 3000

CMD ["npm", "start"]
